<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tourist.ShipmentMobile.CustomerPickerPage"
             xmlns:vm="clr-namespace:Tourist.ShipmentMobile.ViewModels"
             Title="Клиенти">
    <ListView ItemsSource="{Binding Customers}" RowHeight="120" ItemTapped="customersList_ItemTapped">
        <ListView.ItemTemplate>
            <DataTemplate>
                <!--<ViewCell>
                    <Label Text="{Binding Name}" FontSize="36" TextColor="White"  VerticalTextAlignment="Center" Padding="20,0,0,0"/>
                </ViewCell>-->
                 <TextCell Text="{Binding Name}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>
