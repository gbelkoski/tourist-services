<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Tourist.ShipmentMobile.ManageSettingsPage"
             Title="Подесувања">
    <VerticalStackLayout Spacing="20" Margin="10">
        <Entry Placeholder="Url"
               x:Name="txtUrl"
               Text="{Binding ApiBaseUrl}"/>
        <Button Text="Зачувај" Command="{Binding SaveCommand}" />
    </VerticalStackLayout>
</ContentPage>
